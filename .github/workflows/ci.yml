name: ci

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Train baseline (sample)
        run: |
          python scripts/train_baseline.py || true
      - name: Run tests
        run: |
          pytest -q || true

# CI workflow skeleton for StreamScore.
#
# Installs dependencies, optionally downloads/trains on a small sample,
# and runs pytest. This is a placeholder to be completed later.

name: ci
on:
  push:
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      - name: Train (sample) and Test
        run: |
          echo "Placeholder for training on sample and running pytest"


